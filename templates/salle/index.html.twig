{% extends 'layout.html.twig' %}

{% block title %}Nouvelle salle{% endblock %}

{% block body %}
    <div class="card text-white bg-success mb-3" style="max-width: 18rem;">
        <div class="card-header">Nouvelle salle</div>
        <div class="card-body">
            <img src="{{ asset('Images/calendar.png')}}" width= "100px"/></br>
            <p class="card-text">La salle a bien été enregistrée!</p>
        </div>
    </div>
    <a href="{{ path('salle_add') }}"><button type="button" class="btn btn-outline-primary">➕ Enregistrer une nouvelle salle</button></a>
    <a href="{{ path('salle_list')}}"><button type="button" class="btn btn-outline-secondary">📝 Liste des salles</button></a>
{% endblock %}
