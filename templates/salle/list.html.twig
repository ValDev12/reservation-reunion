{% extends 'layout.html.twig' %}

{% block title %}Liste des salles{% endblock %}

{% block body %}
    <h1>
        Les salles de réservation <a href="{{ path('salle_add' ) }}"><img src= "{{ asset('Images/index.png')}}" height="40"></a>
    </h1>
    {% if salles|length > 0 %}
    {% for i in 0..salles|length - 1 %}
    <ul>
        <tr>
            <br>
                Nom de la salle :{{salles[i].libelle }}
                <p></p>
                Nombre de place(s) :{{salles[i].nbPlaces}}
            </br>
            <br>
                <a href="{{ path('salle_update' , {id : salles[i].id}) }}"><img src= "{{ asset('Images/im.jpg')}}" height="30"></a>
                <a href="{{ path('salle_delete' , {id : salles[i].id}) }}"><img src= "{{ asset('Images/images.png')}}" height="30"></a>
            </br>    
        </tr>
    </ul>
    {% endfor %}
    {% endif %}
    <pre>
        Revenir au menu <a href="{{ path('salle')}}"><img src= "{{ asset('Images/i2.jpg')}}" height="30"></a>
    </pre>
{% endblock %}