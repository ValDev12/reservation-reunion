{% extends 'layout.html.twig' %}

{% block title %}Nouveau rôle{% endblock %}

{% block body %}
    <div class="card text-white bg-success mb-3" style="max-width: 18rem;">
        <div class="card-header">Nouveau rôle</div>
        <div class="card-body">
            <img src="{{ asset('Images/calendar.png')}}" width= "100px"/></br>
            <p class="card-text">Le rôle a bien été ajouté!</p>
        </div>
    </div>
    <a href="{{ path('role_add') }}"><button type="button" class="btn btn-outline-primary">➕ Créer une nouveau rôle</button></a>
    <a href="{{ path('role_list')}}"><button type="button" class="btn btn-outline-secondary">📝 Liste des rôles</button></a>
{% endblock %}
