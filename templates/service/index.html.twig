{% extends 'layout.html.twig' %}

{% block title %}Nouveau service{% endblock %}

{% block body %}
    <div class="card text-white bg-success mb-3" style="max-width: 18rem;">
        <div class="card-header">Nouveau service</div>
        <div class="card-body">
            <img src="{{ asset('Images/calendar.png')}}" width= "100px"/></br>
            <p class="card-text">Le service a bien été créé!</p>
        </div>
    </div>
    <a href="{{ path('service_add') }}"><button type="button" class="btn btn-outline-primary">➕ Créer un nouveau service</button></a>
    <a href="{{ path('service_list')}}"><button type="button" class="btn btn-outline-secondary">📝 Liste des services</button></a>
{% endblock %}
